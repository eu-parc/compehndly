# R Usage

Placeholder.

This page will document how to load the R package, call derived-variable
functions, and run examples against Polars-backed data.

Current entrypoint shape in the R package:

```r
compehndly_apply(function_name, ..., .params = list())
```
